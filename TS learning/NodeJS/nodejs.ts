//// Basic
//let num: number = 5
//let str: string = "labas"
//let bul: boolean = true
//let some: any = "Asd"
//// Array
//let nums: number[] = [5, 8, 7, 6, 8]
//let arr: any[] = ["65", true, 5]
//// Tuple
//let arr2: [string, number, boolean] = ["asd", 5, true]

//// Union
//let id: number | string = 10
/*
let num: number = 5
let str: string = "labas"
let bul: boolean = true
let arr: [string, number, boolean, number] = ["hello", 55, true, 11]

if (num > 100) {
    console.log(`true`)
} else console.log("false")
arr.push(`element1`)


let num2: number = 2211221212312312312331221
let num4: string = "Gabrielius"
let num3: number = console.log(number.length)


type UserType = {
    readonly name: string,
    age: number,
    hascat: boolean
    city?: number | string
}

const user: UserType = {
    name: "Gabrielius",
    age: 28,
    hascat: false
}

const UserTwo: UserType = {
    name: "Tanos",
    age: 22,
    hascat: true
}

user.name = "Tomas"



 */
// create object with name, age, city, mobile, array of interests (strings), height, hair color
// make name and age to be read only
// make hair color to be optional
// create variable with array of numbers, add 5 numbers to it
// check if you object with user age is more than 18
// if yes get second number form array with numbers and set it as user age
// create 3 variables with string types, make values to bie various hobbies
// push newly created variables values to user object interests array
// change user height to 180

/*
type User2Type = {
    readonly name: string,
    readonly age:number
    city: string
    mobile: string | number
    interests: string
    height: number
    hair?:string
}
 */
// create interface for object with carModel, carYear, gasolineConsumption, isDiesel
// create object with random car info according to earlier created interface
// create function which takes car object as an argument and returns string as a value
// in function check if car isDiesel
// if yes return car model and carYear combined in string
// if no return car gasolineConsumption combined with isDiesel in string

/*



type cars = {
    carModal: string
    carYear: number
    gasolineConsumption: number | string
    isDiesel: boolean
}
const Toyota: cars ={
    carModal: "Tesla",
    carYear: 20,
    gasolineConsumption: 28 + "L/100km",
    isDiesel: true
}
const Bmw: cars ={
    carModal: "Toyota",
    carYear: 25,
    gasolineConsumption: 15 + "L/100km",
    isDiesel:false
}
 */
////////////////////////////////////////////////////////////07/12/2021



